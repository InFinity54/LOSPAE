{% extends "base.html.twig" %}

{% block title %}Administration > Modération des photos de profil{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="/assets/css/global.css" />
{% endblock %}

{% block body %}
    <div id="app">
        {% include "includes/components/sidebar.html.twig" %}

        <div id="main">
            <header class="mb-3">
                <a href="#" class="burger-btn d-block d-xl-none">
                    <i class="bi bi-justify fs-3"></i>
                </a>
            </header>

            <div class="page-heading">
                <h3>Modération des photos de profil</h3>
                <p class="text-muted">Cette page permet de visualiser les photos de profil de l'ensemble des utilisateurs de LOSPAÉ. Elle permet également de réinitialiser celle d'un utilisateur, si elle est contraire aux règles en vigueur.</p>
            </div>

            <div class="page-content">
                <div class="row">
                    {% for user in users %}
                        <div class="col-sm-12 col-md-6 col-lg-4 col-xl-3">
                            <div class="card">
                                <div class="card-body py-4 px-4">
                                    <div class="d-flex align-items-center">
                                        <div class="avatar avatar-xl">
                                            <img src="/assets/img/avatars/{{ user.avatar }}" alt="Photo de profil de {{ user.firstName }} {{ user.lastName }}" />
                                        </div>
                                        <div class="ms-3 name">
                                            <h5 class="font-bold">{{ user.firstName }} {{ user.lastName }}</h5>
                                            <h6 class="text-muted mb-0">
                                                {% if user.avatar is not same as("default_avatar.svg") %}
                                                    <a href="{{ path("admin_avatar_remove", { id: user.id }) }}" class="btn btn-danger" title="Supprimer la photo de profil de {{ user.firstName }} {{ user.lastName }}">
                                                        <i class="fas fa-trash-alt"></i>
                                                    </a>
                                                {% else %}
                                                    Cet utilisateur n'a pas encore modifié sa photo de profil.
                                                {% endif %}
                                            </h6>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>

            {% include "includes/components/footer.html.twig" %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}