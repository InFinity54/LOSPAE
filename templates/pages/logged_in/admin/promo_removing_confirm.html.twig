{% extends "base.html.twig" %}

{% block title %}Administration > Promotions{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="/assets/css/global.css" />
{% endblock %}

{% block body %}
    <div id="app">
        {% include "includes/components/sidebar.html.twig" %}

        <div id="main">
            <header class="mb-3">
                <a href="#" class="burger-btn d-block d-xl-none">
                    <i class="bi bi-justify fs-3"></i>
                </a>
            </header>

            <div class="page-heading">
                <h3>Suppression d'une promotion</h3>
            </div>

            <div class="page-content">
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h4 class="card-title">Confirmation de l'opération</h4>
                            </div>
                            <div class="card-body">
                                <p>Vous êtes sur le point de supprimer la promotion suivante :</p>

                                <div class="card">
                                    <div class="card-body py-3 px-3">
                                        <div class="d-flex align-items-center">
                                            <div class="ms-3 name">
                                                <h5 class="font-bold">{{ promo.name }}</h5>
                                                <h6 class="text-muted mb-0">
                                                    {{ promo.school.name }}
                                                    <br />{{ promo.school.address }}
                                                    <br />{{ promo.school.postalCode }} {{ promo.school.city }}
                                                </h6>
                                                <h6 class="text-muted mt-3 mb-0">
                                                    {{ promo.students|length }} étudiant{% if promo.students|length > 1 %}s{% endif %}
                                                </h6>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <p class="text-danger">Cette opération est irréversible. Supprimer la promotion entraînera la suppression des affectations des étudiants qui y ont été intégrés, ainsi que leurs historiques de modifications de notes et leur note actuelle. Les enseignants affectés à cette promotion perdront également cette affectation.</p>
                                <p>Êtes-vous sûr de vouloir effectuer cette opération ?</p>
                                <p>
                                    <a href="{{ path("admin_promo_doremove", { id: promo.id }) }}" class="btn btn-primary">Oui, je suis sûr(e).</a>
                                    <a href="{{ path("admin_promos") }}" class="btn btn-secondary">Non, j'ai changé d'avis.</a>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {% include "includes/components/footer.html.twig" %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}