{% extends "base.html.twig" %}

{% block title %}Gestion des critères de notation{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="/assets/css/global.css" />
{% endblock %}

{% block body %}
    <div id="app">
        {% include "includes/components/sidebar.html.twig" %}

        <div id="main">
            <header class="mb-3">
                <a href="#" class="burger-btn d-block d-xl-none">
                    <i class="bi bi-justify fs-3"></i>
                </a>
            </header>

            <div class="page-heading">
                <h3>Suppression d'un critère de notation</h3>
            </div>

            <div class="page-content">
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h4 class="card-title">Confirmation de l'opération</h4>
                            </div>
                            <div class="card-body">
                                <p>Vous êtes sur le point de supprimer définitivement le critère "{{ criteria.name }}" de LOSPAÉ.</p>
                                <p class="text-danger">Cette opération est irréversible. Toutes les données liées à ce critère seront définitivement supprimées de LOSPAÉ.</p>
                                <p>Êtes-vous sûr de vouloir effectuer cette opération ?</p>
                                <p>
                                    <a href="{{ path("teacher_criteria_doremove", { id: criteria.id }) }}" class="btn btn-danger">Oui, je suis sûr(e).</a>
                                    <a href="{{ path("teacher_criterias") }}" class="btn btn-secondary">Non, j'ai changé d'avis.</a>
                                </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {% include "includes/components/footer.html.twig" %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}